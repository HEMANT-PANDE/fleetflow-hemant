# FleetFlow Backend

This is the backend for the FleetFlow application, built with FastAPI.

## Getting Started

1. Install requirements: `pip install -r requirements.txt`
2. Run the application: `uvicorn app.main:app --reload`
